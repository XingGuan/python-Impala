import { Portal } from "@angular/cdk/portal";
import { ApplicationRef, ComponentFactoryResolver, Injector } from "@angular/core";
/**
 *
 */
export declare class PortalHostOverlay extends BMap.Overlay {
    private map;
    private point;
    private hostDomElement;
    private portalHost;
    constructor(componentFactoryResolver: ComponentFactoryResolver, appRef: ApplicationRef, defaultInjector: Injector);
    attach(portal: Portal<any>, lon: number, lat: number): void;
    detach(): void;
    initialize(map: BMap.Map): HTMLElement;
    isVisible(): boolean;
    draw(): void;
    show(): void;
    hide(): void;
}
