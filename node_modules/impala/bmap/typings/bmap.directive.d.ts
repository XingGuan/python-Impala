import { BMapConfig } from './bmap-config';
import { AfterViewInit, ElementRef, NgZone, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
export declare class BMapDirective implements AfterViewInit, OnChanges {
    private mapConfig;
    private elementRef;
    private zone;
    mapLoaded: EventEmitter<BMap.Map>;
    mapCenter: BMap.Point;
    mapZoom: number;
    private map;
    constructor(mapConfig: BMapConfig, elementRef: ElementRef, zone: NgZone);
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
