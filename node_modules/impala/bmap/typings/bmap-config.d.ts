import { InjectionToken } from '@angular/core';
export interface BMapConfig {
    accessKey: string;
    /**
     * default zoom
     */
    zoom?: number;
    /**
     * default center
     */
    center?: number[];
}
export declare let BMAP_CONFIG: InjectionToken<BMapConfig>;
